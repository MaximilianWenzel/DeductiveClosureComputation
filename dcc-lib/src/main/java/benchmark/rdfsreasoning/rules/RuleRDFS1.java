package benchmark.rdfsreasoning.rules;

/**
 * type(d, rdf:type, rdfs:Datatype) :- nothing .
 */
public class RuleRDFS1 {

    // not required, derived while generating internal representation of dataset
}
